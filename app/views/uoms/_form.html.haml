= form_for @uom, :html => {:id => 'uom_from', :class => 'form-horizontal' } do |f|
  %fieldset
    -if @uom.errors.any?
      .row-fluid
        .span6
          #error_explanation
            %h2= "#{pluralize(@uom.errors.count, "error")}"
            %ul
              - @uom.errors.full_messages.each do |msg|
                %li= msg
    
  
    .control-group
      = f.label :name, :class => 'control-label'
      .controls
        = f.text_field :name,  :class => 'text_field', :required => true
    
  .form-actions
    = f.submit 'Save', :class => 'btn btn-primary'
    = link_to 'Cancel', :back, :class => 'btn'





